#
alias git-fetch-all-pull='git fetch --all --prune && git pull origin $(git branch | sed -n -e "s/^\* \(.*\)/\1/p")'

#
alias git-fetch-reset-clean='git fetch origin && git reset --hard origin/master && git clean -f -d'
